<%= form_for(@wine) do |f| %>

<%= render :partial => "form", :locals=>{:f=>f} %>


<%= f.label :type_name %>
<%= f.collection_select :type_id, Type.all, :id, :name %>
<br> <br>

<%= f.submit %>
<% end %>
