<h1><%= @wine.name %></h1>

<h3><%= @wine.type.name %> </h3>
<h3><%= @wine.year %> </h3>
<h3>$<%= @wine.price %> </h3>


<%= link_to "Edit Wine", edit_wine_path(@wine) %>
<%= link_to "Delete Wine", wine_path(@wine), method: :delete %>

<br>
<br>

<% if @user_wine.uncorked == false %>
<%= form_for @user_wine do |f| %>

<%= label_tag :uncorked %>
<%= check_box_tag :uncorked%>

<%= submit_tag %>
<%end %>

<%end%>
